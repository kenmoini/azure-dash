apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: azure-dash
resources:
  - ../kubernetes
  - ingress.yaml
patches:
  # Replace with the UAMI client ID before applying (see README).
  - target: {kind: ServiceAccount, name: azure-dash}
    patch: |-
      - op: add
        path: /metadata/annotations
        value:
          azure.workload.identity/client-id: "00000000-0000-0000-0000-000000000000"
  # The Workload ID webhook only mutates pods carrying this label.
  - target: {kind: Deployment, name: azure-dash}
    patch: |-
      - op: add
        path: /spec/template/metadata/labels/azure.workload.identity~1use
        value: "true"
  - target: {kind: ConfigMap, name: azure-dash-config}
    patch: |-
      - op: replace
        path: /data/AZURE_SUBSCRIPTION_ID
        value: "AZ_SUB_ID"
      - op: replace
        path: /data/AZURE_RESOURCE_GROUP
        value: "AZ_RG_NAME"